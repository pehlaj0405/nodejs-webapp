{
  "name": "my-azure-webapp",
  "version": "1.0.0",
  "description": "Simple Node 20 LTS Express app for Azure Web App deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests defined\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}